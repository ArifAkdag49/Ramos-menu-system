# Build kararları

Faz B'de belirsizlik ya da sapma olduğunda verilen kararlar (BUILD-PROMPT §2.2). Spec ile plan çelişirse spec kazanır.

| Tarih | Görev | Karar | Gerekçe |
|---|---|---|---|
| 2026-09-15 | Faz A | Kurulu olmayan `design:*` skill'leri yerine muadiller: tasarım eleştirisi → `ui-ux-pro-max` (review) + `frontend-design`; erişilebilirlik → `ui-ux-pro-max` a11y kuralları + Playwright'ta `@axe-core/playwright` ile otomatik WCAG AA taraması; UX metinleri → `brand` (ses tonu) + `ui-ux-pro-max` | BUILD-PROMPT §10 "yoksa en yakınını kullan"; kullanıcı Faz A'da onayladı |
| 2026-09-15 | Faz A | Supabase MCP yetkisiz; advisors (`/v1/projects/{ref}/advisors/security` ve `/performance`), TS tipleri ve loglar Management API + PAT ile | BUILD-PROMPT §4: MCP kolaylıktır; uçlar Faz A'da doğrulandı |
| 2026-09-15 | Faz A | Çalışma dalı `build/ramos-v1` (`main`'den); git worktree kullanılmaz | `.env`, Playwright, Docker ve zamanlanmış görev ana klasörde çalışır; commit'ler ayrı dalda kalır |
| 2026-09-15 | Görev 2 | Auth `disable_signup: true` ve `site_url = https://ramos.arxdigitalsevice.com` proje oluşturulur oluşturulmaz ayarlanır; Görev 27 yalnız doğrular | spec §12, §17.1: kayıt kapalı olmalı; açık kaldığı süre sıfırlanır |
| 2026-09-15 | Tümü | Commit son satırı `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` | Oturumun güncel imza yönergesi BUILD-PROMPT §2.3'teki satırın yerine geçer |
| 2026-09-15 | Görev 1 | ESLint yapılandırmasına `**/*.{js,mjs,cjs}` için Node globals eklenir (`globals` paketi) | Plandaki yapılandırma `scripts/*.mjs` dosyalarında `no-undef` verip `npm run check`'i kırardı |
| 2026-09-15 | Görev 1 | Para testindeki € öncesi boşluk U+00A0 (` `) | Intl de-DE çıktısı NBSP üretir |
| 2026-09-15 | Görev 1 | `.gitattributes`: `* text=auto eol=lf`; `*.cmd`, `*.bat` → `eol=crlf` | Bu makinede `core.autocrlf=true`; Linux'a gidecek bash betikleri (Görev 29) CRLF ile bozulur, `.cmd` etiketleri CRLF ister |
| 2026-09-15 | Görev 2 | `.env` Faz A'da oluştu: Görev 2 yalnız boş Supabase anahtarlarını doldurur; `.env.example` tüm anahtarları değersiz listeler | Kullanıcı sırları korunur |
| 2026-09-15 | Görev 2 | Proje anahtarlarında önce eski `anon`/`service_role` JWT anahtarları; yoksa publishable/secret (karar kaydıyla) | `verify_jwt`, Realtime `setAuth` ve Edge Function ortamı en basit hâliyle çalışır |
| 2026-09-15 | Görev 2 | `supabase/tests/helpers/sql.ts` ile `scripts/db.mjs` içindeki Management API çağrısı kopya kalır | `db.mjs`, Görev 26'daki main-guard'a kadar içe aktarılınca CLI'yı çalıştırır |
| 2026-09-15 | Görev 3–9, 26 | "Advisors temiz" = ERROR/WARN yok; INFO'lar değerlendirilir (işaretlenen FK'lara indeks; boş veritabanında `unused_index` ve `daily_counters` politikasızlığı bilinçli) | Boş veritabanında performans INFO'ları kapanamaz |
| 2026-09-15 | Görev 4–7 | `internal.*` dahil tüm fonksiyonlarda `set search_path = ''` | Advisor `function_search_path_mutable` uyarısı; BUILD-PROMPT §6 |
| 2026-09-15 | Görev 6 | `mark_order_served` `in_kitchen → served` geçişini de kabul eder (planın netleştirmesi korunur) | Yalnız içecekli siparişte masa kapanabilsin; spec yasaklamıyor; arayüzde ikincil eylem |
| 2026-09-15 | Görev 4 | `print_jobs` okuma yetkisi admin, waiter, kitchen, printer | Spec §8.2/§8.3 ekranları garson ve mutfağa yazdırma durumunu ve "Tekrar dene"yi gösterir; §12'deki dar satır bunu engellerdi |
| 2026-09-15 | Görev 11 | Tek ESLint yapılandırması kökte: Vite şablonunun `apps/web/eslint.config.js` dosyası kaldırılır, köke `apps/web` için react-hooks + react-refresh bloğu eklenir | Kök `eslint .` ESLint 9 ve 10'da tutarlı çalışsın |
| 2026-09-15 | Görev 11 | `apps/web` typecheck/build `tsc -b` ile | Şablonun çözüm tsconfig'i (`files: []`) ile `tsc --noEmit` hiçbir dosyayı denetlemez |
| 2026-09-15 | Görev 15, 16, 24 | Tasarım kapıları (M3, M4, M6) kontrolcü adımıdır: ekran görüntüleri → muadil skill'lerle eleştiri + axe WCAG AA → kritik/yüksek bulgular için düzeltme → görüntüler yenilenir | BUILD-PROMPT §7, §10 |
| 2026-09-15 | Görev 17 | Fiş meta satırlarında spec'teki "·" ayırıcı: "Bestellung #047 · Runde 2", "15.09.2026 19:42 · Kellner: Ahmet" | Spec §9.2/§9.3 (spec kazanır); CP857'de "·" var |
| 2026-09-15 | Görev 17 | Kaydırma testinin beklenen ilk satırı 49 karakterdi; 48 kolon kuralına göre `…mit Pommes` / `   oder Reis und extra Salat` olarak düzeltilir | Spec §9.1 ve aynı testteki ≤ 48 kontrolüyle çelişiyordu |
| 2026-09-15 | Görev 20 | BOM doğrulaması `[IO.File]::ReadAllBytes(...)[0..2]` ile; zamanlanmış görev denemesi etkileşimsiz kaldırılır | PowerShell 5.1'de `Format-Hex -Count` yok; `uninstall-agent.ps1`'deki Read-Host kullanıcı içindir |
| 2026-09-15 | Görev 19–20 | Canlı veritabanına aynı anda tek ajan; M5 sonunda `printer_host = 192.168.1.250` ve çalışan ajan kalmaz | Karışıklık ve gereksiz baskı önlenir |
| 2026-09-15 | Görev 21 | PrinterCard için başarısız iş listesi sorgusu eklenir | Spec §8.4 iş başına "Tekrar dene" ister; veri katmanı yalnız sayıyordu |
| 2026-09-15 | Görev 26 | VAPID ve WEBHOOK sırları ekrana basılmadan doğrudan `.env` dosyalarına yazılır | Sır kuralı |
| 2026-09-15 | Görev 28 | `.gitignore`'a `!**/.env.*.example` | `apps/web/.env.production.example` aksi hâlde yok sayılırdı |
| 2026-09-15 | Görev 28 | Canlı duman testi `test-e2e-canli` hesabı ve `Test-Tisch-Canli` masasıyla; ardından temizlik yeniden çalışır ve `DB_TESTS_ALLOWED=0` kalır | Mevcut temizlik süzgeçleri (`test-%`) bu veriyi kapsasın |
| 2026-09-15 | Görev 2 | `supabase/migrations/.gitkeep` commit edildi | Temiz klonda `npm run db:apply` klasör olmadığı için ENOENT veriyordu (inceleme bulgusu) |
| 2026-09-15 | Görev 3 | FK indeksleri 0001'e, ilk uygulamadan önce eklenir: sorgu yolundaki FK'lar (`order_items.product_id`, `orders.waiter_id`, `print_jobs.session_id`, `product_ingredients.ingredient_id`, `product_option_groups.group_id`, gerekirse `table_sessions.table_id`); işlem yapan kişi sütunları (`opened_by`, `closed_by`, `ready_by`, `served_by`, `cancelled_by`, `created_by`, `updated_by`, `variant_id`) indekssiz kalır, INFO olarak kabul | Bu sütunların üst kayıtları hiç silinmez; indekslemek yalnız bir INFO uyarısını başka bir INFO'ya çevirirdi. Uygulanan migration değiştirilemediği için karar önceden verilir |
| 2026-09-15 | Görev 4 | 0002, `anon`'un dizi (sequence) yetkilerini de kaldırır: `revoke all on all sequences in schema public from anon` + varsayılan yetkilerde sequence'ler | Spec §5 "anon'un hiçbir erişimi yok"; Görev 3'te `audit_log_id_seq` üzerinde anon yetkisi bulundu |
| 2026-09-15 | Görev 4 | 0002'de açık yetki: `authenticated`'a tablolarda select/insert/update/delete, `service_role`'a tüm yetkiler, ikisine sequence usage/select | Şema, Supabase'in değişen varsayılan yetkilerine (yeni tablolar için 2026-10-30 değişikliği) bağlı kalmasın; geri yükleme yeni projede de çalışsın. Satır erişimini yine RLS belirler |
| 2026-09-15 | Görev 3 → 4 | 0001'deki şema bazlı `revoke execute … from public` PUBLIC'in genel varsayılanını kaldıramaz; kabul edildi. Fonksiyon oluşturan her migration sondaki toplu `revoke … from public, anon` satırını korur ve Görev 4'ten itibaren DB testleri "anon hiçbir public fonksiyonu çalıştıramaz, hiçbir sequence yetkisi yok" değişmezini denetler | 0001 uygulandı, değiştirilemez; genel `alter default privileges` eklenti fonksiyonlarını etkileyebilirdi. Test, unutulan bir revoke'u anında yakalar |
| 2026-09-15 | Tümü | Canlı veritabanında migration öncesi deneme yalnız tek istekte `begin; … ; rollback;` biçiminde ve ardından kalıntı kontrolüyle yapılır; `npm run db:apply` dışında başka DDL çalıştırılmaz | Uygulanan migration değiştirilemediği için deneme değerli; yan etki riski kalıntı kontrolüyle sınırlanır |
| 2026-09-16 | Görev 4 | Advisor uyarısı `auth_leaked_password_protection` kabul edildi (sızmış parola koruması açılmadı) | Özellik yalnız Pro planda; proje Free (0 $ onaylı) ve spec'in 6 haneli PIN'lerini reddederdi. Önlemler: kayıt kapalı, hesapları yalnız admin açar, genel giriş hatası, Supabase IP hız sınırı |
| 2026-09-16 | Görev 4 → tüm RPC görevleri | Advisor uyarısı `authenticated_security_definer_function_executable` bilinçli tasarım olarak kabul: RLS yardımcıları (profiles politikasında özyinelemeyi önlemek için definer şart), takvim yardımcıları (spec §5.3) ve rol kontrollü tüm RPC'ler (spec §6). `anon` için aynı tür uyarı kabul edilmez | Spec'in güvenlik modeli: yetki kontrolü fonksiyon içinde (`internal.require_role`) |
| 2026-09-16 | Görev 5 | `submit_order` idempotency'si eşzamanlı isteklere dayanıklı hâle getirilir: yeni migration (`0003_submit_order_idempotency.sql`) masa satırı kilidini idempotency kontrolünden önce alır; aynı `order_id` ile iki eşzamanlı istek hatasız döner (biri `duplicate: true`), tek sipariş ve tek fiş oluşur | Spec §13: bağlantı koparsa aynı `order_id` ile otomatik tekrar, çift sipariş olmaz. Planın sürümünde ikinci eşzamanlı istek 409 alıyordu |
| 2026-09-16 | Yürütme (kullanıcı talimatı) | **Hızlandırma:** (1) Ön taramada dosya çakışması olmayan görevler paralel yürür, aynı anda en fazla 3 ajan; veritabanına yazan görevler (migration/`db:apply`/seed) asla paralel değil, aynı `npm install`/`package.json` dosyasına dokunan görevler de değil. (2) İnceleme riske göre: SQL, RLS, auth ve fiş mantığı → opus; plandan birebir kod yazılan mekanik görevler → kısa inceleme (sonnet/haiku). (3) Düşük riskli görevlerde (saf TypeScript, arayüz bileşenleri) dry-run ve ek canlı doğrulama yok; testler + `npm run check` yeterli. (4) Bookkeeping commit'i 2–3 görevde bir toplanır; defter notları tek satır. **Değişmeyenler:** testler, TDD, tasarım kapıları (M3/M4/M6), güvenlik kontrolleri (advisors, anon değişmezleri, sır hijyeni) ve her görevin incelemesi | Kullanıcı talimatı: "Hızlanalım. Testleri, tasarım kapılarını ve güvenlik kontrollerini ASLA atlama" |
| 2026-09-16 | Görev 6 → 7 | `close_table_session` ilk iş olarak masa satırı kilidini alır (`submit_order` ile aynı kilit; sıra masa → oturum → siparişler) | İnceleme bulgusu: kilitsiz hâlde bir garson masayı kapatırken diğerinin siparişi kapanan oturuma düşebiliyor; sipariş hesaba girmiyor, hiçbir ekranda görünmüyor ama yazdırılıyor (spec §13 "sipariş kaybolmaz") |
| 2026-09-16 | Görev 6 → 7 | `cancel_order_item` otomatik iptali yalnız `in_kitchen`/`ready` durumundan yapar | Spec §3.5 bu geçişi yalnız o iki durumdan tanımlıyor; teslim edilmiş sipariş `served` kalır (kalemler iptal olur) |
| 2026-09-16 | Görev 17 | `wrap()` satıra sığmayan tek kelimeyi bölerek yazar (karakter kaybı ve boş ilk satır yok) | İnceleme, uzun Almanca bileşik ürün adında kelimenin kuyruğunun sessizce silindiğini üretti; fiş yanlış basardı. R16'daki test beklentisi değişmeden geçerli |
| 2026-09-16 | Görev 17 | Meta bloğu ile kalemler arasına ayraç (`rule`) eklenir | Spec §9.3 örnek fişinde ayraç var; plan metni atlamıştı |
| 2026-09-16 | Görev 7 | `complete_print_job` yalnız `printing` durumundaki ve (ajan kimliği verilmişse) o ajana ait işi kapatır; aksi hâlde `job_not_printing` hatası. Yeni migration `0005_print_queue_realtime_fix.sql` + kuyruk indeksi | İnceleme bulgusu: 60 sn'lik yeniden sahiplenme ile takılan ajanın geç gelen yanıtı, başka ajanın bastığı işi tekrar kuyruğa alıp çift fiş bastırabiliyordu |
| 2026-09-16 | Görev 10 | `.env.example` içindeki değersiz `ADMIN_PASSWORD=` satırı kalır (gerçek `.env`'den admin oluşturulduktan sonra silindi) | Yeni bir projeye geri yükleme yapılırken ilk admin yine bu anahtarla oluşturulacak; örnek dosya anahtarları belgeler, sır içermez |
| 2026-09-16 | Görev 7 | `complete_print_job`'ın üç argümanlı imzası kaldırıldı; tek imza `(p_job_id, p_ok, p_error, p_agent_id)` | Varsayılan değerli dördüncü parametre ayrı bir imza yaratıyor ve üç argümanlı çağrılar belirsiz kalıyordu. Henüz dışarıdan çağıran yok (ajan Görev 19'da yazılacak) |
| 2026-09-16 | Görev 19 | Yazdırma ajanı `claim`/`complete` çağrılarında çalıştırmaya özgü kimlik kullanır (`${AGENT_ID}#${başlangıç damgası}`), `agent_heartbeat` sade `AGENT_ID` gönderir; ajan tek örnek çalışır | Aynı PC'de takılmış eski süreç ile yeniden başlayan süreç aynı kimliği paylaşırsa sahiplik kontrolü devre dışı kalıyor ve çift baskı yolu açık kalıyordu. Plan 3 ve Plan 1 metinleri düzeltildi |
| 2026-09-16 | Görev 7 | Test zamanlamaları: geri çekilme üst sınırı veritabanı zaman damgasına göre (+9 sn), realtime olay beklemesi 15 sn, `sql.ts` 429 geri çekilmesi ~15,5 sn | Plandaki üst sınır "kalan süreyi" ölçtüğü için hiç başarısız olamıyordu; gecikme altında gerçek bir kararsızlık vardı. Yeniden deneme yalnız 5xx/429/yetki hatasında, SQL hatasında asla |
| 2026-09-16 | Görev 17 | `TISCH x -> TISCH y` satırı da çift genişlikte (24 karakter bütçesi) | Masa satırıyla aynı tür; 18 karakter = 36 nokta, bütçe içinde |
| 2026-09-16 | Görev 9 | `errors.test.ts` içinde `/// <reference types="node" />` satırı kabul edildi; `packages/shared/tsconfig.json`'a `"types": ["node"]` eklemek sona bırakıldı | Katı tsc, Bundler çözümlemesinde `node:` importlarını bulamıyordu; ajan kendi dosya kapsamı dışına çıkmadı |
| 2026-09-16 | Görev 9 | `1b99bd0` commit'i `Co-Authored-By: Claude Sonnet 5` satırıyla kaldı (olması gereken: `Claude Opus 5 (1M context)`) | Commit dal ucunda değil ve başka ajanlar aynı anda commit atıyor; imza satırı için geçmişi yeniden yazmak gerçek işi riske atardı. Sonraki görevlerde satır birebir kopyalanacak |
| 2026-09-16 | Görev 17 | TESTDRUCK tarih satırı basar; çift genişlikli masa satırı 24, meta/`Drucker:` satırları 48 kolon bütçesine göre kaydırılır; STORNO'da tarih + garson satırı kalır | Spec §9.4 "örnek bir sipariş ve tarih"; uzun masa adı 48 noktalık satırı sessizce taşırıyordu; iptalin ne zaman verildiği mutfak için bilgi |
| 2026-09-16 | Görev 6 → 7 | `report_range.by_hour` yalnız iptal edilmemiş siparişleri sayar; düzeltme `0004_order_lifecycle_report_fix.sql` ile Görev 7'de uygulanır | Plandaki SQL saatlik dağılımda iptalleri sayıyordu; `orders`, `items` ve `value_cents` saymıyor — rapor kendi içinde tutarsızdı |
| 2026-09-16 | Görev 6 | `cancel_order_item` STORNO fişini yalnız sipariş `in_kitchen` ya da `ready` iken kuyruğa alır (planın daraltması korundu) | Teslim edilmiş siparişte mutfağın yapacağı işlem yok; kâğıt gürültüsü yerine iz `audit_log` ve admin ekranında kalır. Spec §6'daki özet satırdan sapma, son raporda belirtilir |
| 2026-09-16 | Görev 7 | `reprint_order` orijinal fiş işinin payload'unu kopyalar (`kind: 'reprint'`, `reprintOf: <orijinal tür>`); güncel durumdan yeniden üretmez | Spec §6.2 "tekrar baskı birebir aynı olur, baskı anındaki masa adı korunur" ve §9.4 "NACHDRUCK: orijinal fiş" açık; plan güncel durumdan üretiyordu — spec kazanır |
| 2026-09-16 | Görev 10 | `deploy-function.mjs` yayından önce `GET /v1/projects/{ref}` ile projenin adını beklenenle (`ramos-siparis`, `SUPABASE_PROJECT_NAME` ile geçersiz kılınabilir) karşılaştırır; `SUPABASE_PROJECT_REF`/`SUPABASE_ACCESS_TOKEN` zorunlu; doğrulama isteği hata verirse yayın durur. Hata yolları `process.exit` yerine `process.exitCode` kullanır | İnceleme bulgusu: görev raporu betikte var olmayan bir "yanlış-proje kontrolü"nden söz ediyordu. Betik `service_role` yetkili bir fonksiyonu yayınlıyor ve aynı hesapta dokunulmaması gereken `Cicek-web` projesi var; ref boşken istek `/projects/undefined/...` adresine gidiyordu. Ani `process.exit` açık soketle Windows'ta libuv assertion'ı tetikleyip çıkış kodunu 127 yapıyordu |
| 2026-09-16 | Görev 18 | Kodlayıcı kendi satır sarmasını yapmaz; sarma, girinti ve 48 kolon bütçesi `@ramos/shared`'ın `renderTicket` sözleşmesidir, kodlayıcı `Line[]`'i birebir bayta çevirir | İnceleme bayt düzeyinde gösterdi: kütüphanenin `wrap()`'i satır başındaki boşlukları atıyordu, "   OHNE: …" kağıda girintisiz basılıyordu ve sarılmış kalemin devam satırı yeni kalem gibi görünüyordu (mutfak hatası riski). Ekran önizlemesi ile kağıt da ayrışıyordu |
| 2026-09-16 | Görev 18 | Kod sayfası adı ile numarası tek eşlemeden türetilir (0=cp437, 16=windows1252, 19=cp858, 61=cp857, 91=windows1254); bilinmeyen numara açık hata verir | Spec §10.4'ün "numarayı 91 yap" kurtarma yolu yalnız `ESC t` baytını değiştiriyor, bayt tablosunu değiştirmiyordu: yazıcı WPC1254'e geçerken baytlar CP857'den üretiliyor ve tüm Türkçe karakterler bozuluyordu |
| 2026-09-16 | Görev 18 | Ön durum sorgusu, fiş payload'u ve son durum sorgusu TEK TCP bağlantısı üzerinden yapılır; gönderim yolu zaman aşımıyla sınırlı ve hata gerçekten yayılır | Spec §10.3.3 tek bağlantı diyor, Xprinter aynı anda tek oturum kabul ediyor; ayrıca `sendBytes` yalnız connect'i sınırlıyordu ve yazıcı FIN döndürmezse sonsuza kadar asılıyordu — Görev 19 döngüsü kalıcı kilitlenir, teslim edilmemiş iş "başarılı" sayılırdı |
| 2026-09-16 | Görev 18 | `ESC t 61` fiş başına bir kez gönderilir (eskiden her çizgiden sonra) | Çizgiler artık ham ASCII; kod sayfasını sıfırlayan bir şey kalmadı. Yeniden inceleme tek `ESC @` + tek `ESC t` ile doğruladı |
| 2026-09-16 | Görev 11 | Giriş alanı hesap türü bilinene kadar `inputMode="numeric"` kullanmaz; etiket hem PIN'i hem parolayı karşılar | Admin aynı ekrandan ≥10 karakterli parolayla giriyor (spec §8.1/§12); rakam tuş takımı Android Chrome'da adminin telefondan girişini tamamen engelliyordu |
| 2026-09-16 | Görev 11 | Profil okuması BAŞARISIZ olduğunda oturum korunur ve yeniden deneme sunulur; çıkış yalnız okuma başarılıyken satır yoksa ya da profil uygun değilse yapılır | `loadProfile` hatayı atıyordu: "yetkisiz" ile "istek başarısız" ayırt edilemiyordu. Free proje 7 gün sonra uykuya geçtiğinde ya da restoran Wi-Fi'si dalgalandığında garson vardiya ortasında PIN girmek zorunda kalırdı (spec §8.1 ihlali) |
| 2026-09-16 | Görev 11 | Playwright yalnız `TEST_USER_PASSWORD`'ü yükler, kök `.env`'nin tamamını değil | `webServer` ile başlayan Vite süreci `SUPABASE_SERVICE_ROLE_KEY`, PAT, DB parolası ve SSH anahtar yolunu miras alıyordu; bugün sızıntı yok ama ileride bir `envPrefix`/`define` değişikliği bunu gerçek sızıntıya çevirir |
| 2026-09-16 | Görev 11 | i18next tip artırımı (spec §15 "anahtarlar tipli") Görev 15'e ertelendi | Kataloglar T13–T15 boyunca büyüyor; şimdi sabitlemek her görevde tip gürültüsü yaratır. Ledger'da izleniyor |
| 2026-09-16 | Süreç | Paralel modda plan dosyası onay kutularını yalnız kontrolör işaretler | Aynı anda 3 ajan aynı plan dosyasına yazarsa commit çakışması ve karışık diff üretir; global-constraints'in bu maddesi paralel modda kontrolöre devredilir |
| 2026-09-16 | Süreç (elektrik kesintisi) | Kesintide yakalanan commit edilmemiş iş, sahibine göre ayrıldı: TAMAMLANMIŞ olan (T16 düzeltme turu) doğrulanıp commit edildi, YARIM olanlar (T19 düzeltme turu 3, T15) BASE commit'ine geri alınıp sıfırdan yeniden başlatıldı. Geri alınan her şey önce `scratchpad/kesinti-yedek/` içine yedeklendi | Kullanıcı talimatı; ayrıca T19'un yarım hâli testsizdi ve `stopGraceMs` kaldırıldığı için typecheck'i kırıyordu — testsiz kalan yazdırma mantığı çift fiş riskini kanıtsız bırakır |
| 2026-09-16 | Görev 16 | `KitchenPage` realtime aboneliğini kendisi açmaz; tek kaynak `ConnectionBanners` | İnceleme bulgusu: sayfa + şerit aynı dört konuya abone olunca mutfak tableti vardiya boyunca 8 kanal tutuyordu; Free planda eşzamanlı bağlantı bütçesi tüm garson telefonlarıyla paylaşılıyor |
| 2026-09-16 | Görev 16 | Mutfağın dört mutasyonu (`mark_order_ready`, `undo_order_ready`, `retry_print_job`, `reprint_order`) `onError` ile hata toast'ı gösterir; daha geniş "uygulama genelinde hata kanalı yok" boşluğu final incelemeye taşındı | Sessiz başarısızlıkta kart bir sonraki refetch'te eski hâline dönüyor ve mutfak HAZIR'a ikinci kez basıyordu |
| 2026-09-16 | Görev 19 | R71: ajanın her Supabase RPC **denemesi** istemci tarafında zaman aşımıyla sınırlanır, deneme **sayısı** sınırsız kalır (R68 korunur) | `rpc()` çağrılarında istemci zaman aşımı yoktu; yarı açık TCP'de istek ne çözülür ne reddedilir (tek sınır undici'nin ~300 sn'si). Fiş basılıp onay askıda kalır, süreç yeniden başlarsa (watchdog ya da operatör) yeni kimlikle 60 sn'lik reclaim aynı fişi İKİNCİ KEZ bastırır |
| 2026-09-16 | Görev 19 | R78: zaman aşımı çağrı başına — `complete_print_job` 10 sn, `claim`/`heartbeat`/`settings`/`signIn` 25 sn | Tek sabit 10 sn livelock açtı: `claim_print_job` önce işi `printing` yapıp `claimed_at = now()` damgalıyor; 10 sn'de iptal edilen her deneme damgayı tazelediği için gidiş-dönüş sürekli >10 sn olan bir ağda fiş ASLA basılmıyordu. 60 sn'lik pencereden kısa olmak yalnız `complete` için doğruluk gereği |
| 2026-09-16 | Görev 19 → 21 | R79: takılı onay işareti (`printer_status.last_error`) yalnız DB'ye yazılır; operatöre gösterilmesi Görev 21'e taşındı | İnceleme, "KDS/admin ekranında görünür olur" iddiasının yanlış olduğunu gösterdi — `derivePrinterProblem()` bu alanı hiç okumuyor. `apps/web` o sırada başka ajandaydı; yanlış iddia kayıttan temizlendi, gereksinim Görev 21'e eklendi |
| 2026-09-16 | Süreç (paralel mod) | Kırmızı kanıtı `git show <commit>:<yol>` + scratchpad kopyasıyla üretilir; `git stash` / `git checkout <yol>` / `git restore` ile toplu geri alma yasak | Bir ajan `git stash` kullandı; komut depo genelinde çalışır ve o sırada `apps/web`'de yazan başka bir ajanın commit edilmemiş işini de kapsardı (bu kez zarar çıkmadı, ağaç doğrulandı) |
| 2026-09-16 | Görev 20 | R82: kaldırma betiği `agent.lock` PID'ini otoriter alır, yolu normalize eder, 0 süreç öldürdüğünde uyarır ve silmeden önce klasörün gerçekten ajan kurulumu olduğunu doğrular | Desen yolun yazımına duyarlıydı: `-InstallDir "C:/Ramos"` gibi bir yazımla görev ve klasör silinir ama `node.exe` öksüz kalıp fiş basmaya devam ederdi. Canlı kanıtlandı: ham desen 0 süreç, normalize desen 1 süreç |
| 2026-09-16 | Görev 20 | R83: kurulum, kopyaladığı `.env`'deki `AGENT_ID`'yi makineye özgü değere yeniden yazar | Geliştiricinin `ramos-pc-1` kimliği restoran PC'sine gidiyordu; `agent_heartbeat` sade `AGENT_ID` gönderdiği için iki makine tek `printer_status` satırını ezer ve ikisi de aynı kuyruktan iş kapardı → restoranın fişi geliştirme PC'sindeki yazıcıdan çıkardı |
| 2026-09-16 | Görev 20 | R84: `run-agent.cmd` beklemesi konsoldan bağımsız (`ping`), art arda 3 hızlı çıkışta `logs\FATAL.txt` bırakıp durur | `-Hidden` görevde kurtarılamaz hata (bozuk `.env`, kilit reddi, PATH'te node yok) sonsuza dek sessizce süreç doğuruyordu; S4U'ya çevrilirse `timeout` anında dönüp döngüyü %100 CPU'ya oturtuyordu |
| 2026-09-16 | Görev 20 | R85: Linux tek-örnek kilidi `/proc/<pid>/stat` + `/proc/<pid>/cmdline` ile bayat kilidi ayırt eder (Görev 30'a ertelenmedi) | `processStartTime()` Linux'ta `null` döndüğü için kilit her zaman "aynı süreç" sayılıyordu; kalıcı kilit + PID çakışmasında `Restart=always` sonsuz döner, `StartLimitBurst` eşiğini bile tetiklemez → hiç fiş basılmaz ve kimse fark etmez |
| 2026-09-16 | Görev 19 → 21 | R79 Görev 21'de kapatıldı: `derivePrinterProblem`'e `complete_stuck` dalı, `PrinterCard`'da süre + yönerge; ham hata metni ekrana basılmaz | Takılı onay işareti yalnız veritabanına yazılıyordu, hiçbir ekran okumuyordu |
| 2026-09-16 | Yürütme (kullanıcı talimatı) | **Hızlandırma 3 — test hafifletme:** (1) Arayüz görevlerinde (21–25) TDD zorunlu değil; testler koddan SONRA ve yalnız mantık için (hook, doğrulayıcı, eşleyici, dosya adı eşleştirme) — salt yerleşim/görünüm bileşenlerine render testi yazılmaz. (2) Düzeltme turlarında yalnız etkilenen workspace'in ilgili test dosyaları + typecheck; tam `npm run check` görev kapanışında bir kez. (3) E2E görev sırasında yalnız ilgili spec ve yalnız `phone`; üç ekran boyutu yalnız tasarım kapılarında ve Görev 27'de. (4) Migration/RPC'ye dokunmayan görevlerde `db:test` koşulmaz. (5) İncelemeciler kırmızı testi ayrı kopyada yeniden ÜRETMEZ ve mutasyon denemesi yapmaz — istisna: print-agent, veritabanı, yetki/güvenlik kodu. **DEĞİŞMEYENLER:** db testleri (RLS, RPC, fiş kuyruğu), print-agent testleri, shared fiyat/sepet testleri, güvenlik kontrolleri (advisors, anon değişmezleri, sır hijyeni), tasarım kapıları M4/M6, Görev 27 tam doğrulama | Kullanıcı, önceki "testleri asla atlama" talimatını kendi kararıyla bu kapsamda güncelledi; riskli alanlar (para, yetki, baskı, veritabanı) tam korumada kalıyor |
