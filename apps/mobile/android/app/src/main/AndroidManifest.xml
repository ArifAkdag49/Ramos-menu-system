<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".RamosApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Arka plan yazıcı istasyonu: WebView'dan bağımsız fiş basan ön plan servisi. APK Play dışı
             dağıtılır; specialUse türü + gerekçe özelliği (Android 14+ zorunlu). -->
        <service
            android:name=".StationService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Restoran yazıcı istasyonu: sunucudaki sipariş fişlerini alıp yerel ağdaki fiş yazıcısına (TCP 9100 / TLS 9143 / Epson ePOS-Print 443) sürekli gönderir" />
        </service>

        <!-- Açılışta / uygulama güncellenince istasyon açık bırakılmışsa servisi yeniden başlatır.
             Yayınları sistem gönderir: dışa açık olması gerekmez. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- FCM: uygulama arka plandayken gelen "notification" iletileri bu kanal ve ikonla gösterilir.
             Kanal RamosApplication.onCreate'te oluşturulur. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="ramos_ready" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_ramos" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />
    </application>

    <!-- İzinler -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Çevrimdışı algılama ve yazıcı bağlantısından önce ağ durumu -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Android 13+ bildirim izni (PushNotifications.requestPermissions ile istenir) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Yeni bildirim gelince titreşim -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Arka plan yazıcı istasyonu: ekran kapalıyken CPU uyanık (PARTIAL_WAKE_LOCK, WifiLock) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Arka plan yazıcı istasyonu (StationService): ön plan servisi, specialUse türü -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Cihaz açılınca istasyonu yeniden başlatmak için -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- "Pil optimizasyonundan muaf tut" isteği (Doze'da uygulama kısıtlanmasın) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Ağda yazıcı arama: SNMP yayını ve Bonjour/mDNS cevapları Wi-Fi çoklu yayın süzgecine takılmasın -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
</manifest>
